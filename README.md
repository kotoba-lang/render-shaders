# kotoba-lang/render-shaders

**SSoT** for `kami.render-shaders` (ADR-2607102200 addendum 5). Extracted from webgpu vendor copy.

| Namespace | Role |
|---|---|
| `kami.render-shaders` | Implementation |
| `kotoba.render-shaders` | Compatibility facade |

`webgpu` depends on this package; do not re-vendor.
